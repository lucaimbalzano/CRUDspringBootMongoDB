
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            repository:
              query: DEBUG
spring:
  data:
    mongodb:
      uri: 'mongodb://localhost:8080/getAllUsers'

